<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="maintenance.list">
      <field name="name">maintenance list</field>
      <field name="model">maintenance.maintenance</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="maintenance.action_window">
      <field name="name">maintenance window</field>
      <field name="res_model">maintenance.maintenance</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="maintenance.action_server">
      <field name="name">maintenance server</field>
      <field name="model_id" ref="model_maintenance_maintenance"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="maintenance" id="maintenance.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="maintenance.menu_1" parent="maintenance.menu_root"/>
    <menuitem name="Menu 2" id="maintenance.menu_2" parent="maintenance.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="maintenance.menu_1_list" parent="maintenance.menu_1"
              action="maintenance.action_window"/>
    <menuitem name="Server to list" id="maintenance" parent="maintenance.menu_2"
              action="maintenance.action_server"/>
    -->
  </data>
</odoo>